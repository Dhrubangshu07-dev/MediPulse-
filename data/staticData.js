/**
 * staticData.js
 *
 * ⚠️  ALL fake business data has been removed.
 * The application is database-driven. These arrays are intentionally empty.
 * If the database is unavailable, the API returns a real error — never fake data.
 */

const staticDoctorProfiles = [];
const staticAppointments   = [];
const staticUsers          = [];

module.exports = { staticDoctorProfiles, staticAppointments, staticUsers };
